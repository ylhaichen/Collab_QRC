# Nav2 full-stack config for Go2W (single robot scaffold).
# Plugins: SmacPlannerHybrid (REEDS_SHEPP) + DWB local planner with
# polygon footprint + behavior_server + lifecycle_manager.
#
# Why this stack:
#   - SmacPlannerHybrid uses (x, y, θ) state space + ReedsShepp shots,
#     plans through narrow corridors that disk-inflation A* cannot.
#   - DWB samples velocity space, simulates each candidate over a short
#     horizon, validates oriented footprint at every sample, picks the
#     best. Zero "pure-pursuit overshoot" because there's no path-tracker
#     loop — controller searches velocities directly.
#   - Polygon footprint (0.65 × 0.45 m rectangle) replaces the disk
#     inflation that broke our previous A* plans.
#
# Topics this config assumes:
#   /robot_a/map               (OccupancyGrid from octomap_server)
#   /robot_a/odom/nav          (from slam_odom_relay, → "odom" via remap)
#   /robot_a/scan_3d           (from pointcloud_to_laserscan)
#   /tf, /tf_static            (namespaced via launch remap)

# ============================================================
# COSTMAPS
# ============================================================
local_costmap:
  local_costmap:
    ros__parameters:
      use_sim_time: true
      update_frequency: 5.0
      publish_frequency: 2.0
      global_frame: odom
      robot_base_frame: base_link
      rolling_window: true
      width: 6
      height: 6
      resolution: 0.05
      # 2026-04-28: switched from robot_radius=0.40 (16-pt circle approx)
      # to a real rectangular polygon. Value 0.70 × 0.40 m (corners at
      # ±0.35, ±0.20) matches what `nav2_params_sim.yaml` already used —
      # only verified value in the repo. Physical meaning: wheelbase
      # 0.387 m + ~0.16 m sensor/tail buffer (front+rear); track 0.284 m
      # + ~0.06 m leg-swing buffer (per side). With consider_footprint=
      # true on the MPPI ObstaclesCritic, the controller checks the
      # *rotated rectangle* per predicted pose instead of a 0.40 m
      # circle. demo3_mixed 0.425 m corridor: axis-aligned body has
      # 0.125 m clearance/side; circle approximation had 0 → MPPI always
      # rejected → "stamping" failure.
      footprint: "[[0.35, 0.20], [0.35, -0.20], [-0.35, -0.20], [-0.35, 0.20]]"
      footprint_padding: 0.03
      plugins: ["obstacle_layer", "inflation_layer"]
      obstacle_layer:
        plugin: "nav2_costmap_2d::ObstacleLayer"
        enabled: true
        observation_sources: scan peer_scan
        scan:
          topic: /robot_a/scan_3d
          max_obstacle_height: 1.5
          clearing: true
          marking: true
          data_type: "LaserScan"
          raytrace_max_range: 6.0
          obstacle_max_range: 5.0
        # Synthetic teammate body sector. robot_self_filter keeps peer
        # geometry out of SLAM; this puts only peer occupancy back into the
        # local controller costmap so MPPI is not blind during execution.
        peer_scan:
          topic: /robot_a/peer_scan
          max_obstacle_height: 1.5
          clearing: true   # 2026-04-30: was false; peer is dynamic so old positions must be raytrace-cleared, otherwise costmap accumulates "peer trail" obstacles → late-stage RViz clutter + coverage stall
          marking: true
          data_type: "LaserScan"
          raytrace_max_range: 6.0
          obstacle_max_range: 6.0
      inflation_layer:
        plugin: "nav2_costmap_2d::InflationLayer"
        # Body diagonal = √(0.325² + 0.225²) ≈ 0.40 m. Inflation must be
        # ≥ this so MPPI's cost field rejects trajectories that pass the
        # body's CENTER within 0.45 m of an obstacle — at any yaw the
        # rotated-rectangle body would clip. demo3_mixed sw_v_2 corridor
        # 2026-04-28: with 0.30 m inflation, SmacHybrid path centerline
        # ran along x=5.0 (the wall), body half-width 0.225 m put the
        # FL wheel at x=5.225 — INSIDE sw_v_2. MPPI followed the path
        # exactly into the wall.
        inflation_radius: 0.45
        cost_scaling_factor: 3.0
      always_send_full_costmap: true

global_costmap:
  global_costmap:
    ros__parameters:
      use_sim_time: true
      update_frequency: 1.0
      publish_frequency: 1.0
      global_frame: map
      robot_base_frame: base_link
      # See local_costmap footprint comment above. Same rectangle for global.
      footprint: "[[0.35, 0.20], [0.35, -0.20], [-0.35, -0.20], [-0.35, 0.20]]"
      footprint_padding: 0.03
      resolution: 0.05
      track_unknown_space: true
      plugins: ["static_layer", "inflation_layer"]
      static_layer:
        plugin: "nav2_costmap_2d::StaticLayer"
        # Subscribe to our octomap-published /robot_a/map.
        map_topic: /robot_a/map
        map_subscribe_transient_local: true
      inflation_layer:
        plugin: "nav2_costmap_2d::InflationLayer"
        # Body half-width 0.225 m. Inflation 0.30 m gives the planner a
        # small comfort margin; SmacHybrid's own footprint check during
        # search is the real safety guarantee.
        inflation_radius: 0.30
        cost_scaling_factor: 3.0
      always_send_full_costmap: true

# ============================================================
# PLANNER SERVER (SmacPlannerHybrid, REEDS_SHEPP)
# ============================================================
planner_server:
  ros__parameters:
    use_sim_time: true
    expected_planner_frequency: 1.0
    planner_plugins: ["GridBased"]
    GridBased:
      plugin: "nav2_smac_planner/SmacPlannerHybrid"
      downsample_costmap: false
      downsampling_factor: 1
      tolerance: 0.4
      allow_unknown: true
      max_iterations: 80000
      max_planning_time: 1.5
      motion_model_for_search: "REEDS_SHEPP"   # supports forward + reverse
      angle_quantization_bins: 36              # 10° bins
      analytic_expansion_ratio: 3.5
      analytic_expansion_max_length: 3.0
      # Tight turning radius so the planner can plan sharp turns. Go2W
      # skid-steer can essentially pivot in place; 0.30 m approximates
      # this in a Reeds-Shepp formalism (true 0 is degenerate).
      minimum_turning_radius: 0.30
      reverse_penalty: 2.0
      change_penalty: 0.0
      non_straight_penalty: 1.20
      cost_penalty: 5.0  # 2026-04-30: was 2.0; raised to push SmacHybrid path through doorway centerlines, not wall-hug
      retrospective_penalty: 0.015
      lookup_table_size: 20.0
      cache_obstacle_heuristic: true
      smooth_path: true
      smoother:
        max_iterations: 1000
        w_smooth: 0.3
        w_data: 0.2
        tolerance: 1.0e-10

# ============================================================
# CONTROLLER SERVER (DWB with oriented footprint check)
# ============================================================
controller_server:
  ros__parameters:
    use_sim_time: true
    controller_frequency: 20.0
    min_x_velocity_threshold: 0.001
    min_y_velocity_threshold: 0.001
    min_theta_velocity_threshold: 0.001
    progress_checker_plugin: "progress_checker"
    goal_checker_plugins: ["goal_checker"]
    controller_plugins: ["FollowPath"]
    progress_checker:
      plugin: "nav2_controller::SimpleProgressChecker"
      required_movement_radius: 0.3
      movement_time_allowance: 12.0
    goal_checker:
      plugin: "nav2_controller::SimpleGoalChecker"
      xy_goal_tolerance: 0.40
      yaw_goal_tolerance: 0.50
      stateful: true
    FollowPath:
      plugin: "nav2_mppi_controller::MPPIController"
      time_steps: 56
      model_dt: 0.05
      batch_size: 1000
      vx_std: 0.20
      vy_std: 0.0   # 2026-04-30 Stage 8: was 0.20; both robots are diff-drive (motion_model: DiffDrive) so vy is hardware-zero — sampling vy noise only wasted batch slots and frequently produced strafing trajectories that the ObstaclesCritic instantly rejected near corners (15-23 Optimizer reset events per 180s trial in N=10 ablation)
      wz_std: 0.40
      # 2026-04-30 Stage 3a: was 0.50 / 0.5; lowered to 0.30 / 0.30 to match
      # Go2 because Go2W high-speed wheel skid produced IMU noise that Fast-LIO
      # over-trusted (acc_cov=0.1 since fixed to 0.3) → yaw catastrophic
      # divergence on robot_a in ~33% of 180s trials (Stage 2 trial 3).
      vx_max: 0.30
      vx_min: -0.10
      vy_max: 0.0
      wz_max: 0.8
      ax_max: 0.30
      ax_min: -0.6
      ay_max: 0.0
      az_max: 1.5
      iteration_count: 2  # 2026-04-30 Stage 8: was 1; one more refinement step gives the optimizer a second chance before reset, halving Optimizer reset events at MPPI 20 Hz controller_frequency × batch=1000
      prune_distance: 1.7
      transform_tolerance: 0.2
      temperature: 0.3
      gamma: 0.015
      motion_model: "DiffDrive"
      visualize: true
      regenerate_noises: false
      TrajectoryVisualizer:
        trajectory_step: 5
        time_step: 3
      AckermannConstrains:
        min_turning_r: 0.3
      critics:
        - "ConstraintCritic"
        - "ObstaclesCritic"
        - "GoalCritic"
        - "GoalAngleCritic"
        - "PathAlignCritic"
        - "PathFollowCritic"
        - "PathAngleCritic"
        - "PreferForwardCritic"
      ConstraintCritic:
        enabled: true
        cost_power: 1
        cost_weight: 4.0
      GoalCritic:
        enabled: true
        cost_power: 1
        cost_weight: 5.0
        threshold_to_consider: 1.4
      GoalAngleCritic:
        enabled: true
        cost_power: 1
        cost_weight: 3.0
        threshold_to_consider: 0.5
      PreferForwardCritic:
        enabled: true
        cost_power: 1
        cost_weight: 5.0
        threshold_to_consider: 0.5
      ObstaclesCritic:
        enabled: true
        cost_power: 1
        repulsion_weight: 1.5
        critical_weight: 8.0
        # 2026-04-28: enabled. Tested on humble 1.1.20 — the prior
        # "throws at configure-time" comment turned out to be a polygon-
        # parsing fail when the yaml used `robot_radius:`; with a real
        # `footprint:` polygon param on both costmaps, MPPI reads a 4-pt
        # rectangle at configure and FootprintCollisionChecker initialises
        # cleanly. Effective rejection envelope is now the *rotated body*
        # (0.65 × 0.45 m) plus collision_margin_distance, instead of a
        # 0.40 m circle + 0.20 m margin. Lets the robot commit to 0.42 m
        # corridors that the planner can already plan.
        consider_footprint: true
        collision_cost: 10000.0
        # Tight margin once consider_footprint is on — MPPI already does the
        # rotated-polygon check; this is just a 3 cm safety pad against
        # discretisation/jitter.
        collision_margin_distance: 0.03
        near_goal_distance: 0.5
      PathAlignCritic:
        enabled: true
        cost_power: 1
        cost_weight: 20.0
        max_path_occupancy_ratio: 0.05
        trajectory_point_step: 4
        threshold_to_consider: 0.5
        offset_from_furthest: 20
        use_path_orientations: false
      PathFollowCritic:
        enabled: true
        cost_power: 1
        cost_weight: 5.0
        offset_from_furthest: 5
        threshold_to_consider: 1.4
      PathAngleCritic:
        enabled: true
        cost_power: 1
        cost_weight: 2.0
        offset_from_furthest: 4
        threshold_to_consider: 0.5
        max_angle_to_furthest: 1.2
        mode: 0

# ============================================================
# BEHAVIOR SERVER (recovery primitives)
# ============================================================
behavior_server:
  ros__parameters:
    use_sim_time: true
    costmap_topic: local_costmap/costmap_raw
    footprint_topic: local_costmap/published_footprint
    cycle_frequency: 10.0
    behavior_plugins: ["spin", "backup", "wait"]
    spin:
      plugin: "nav2_behaviors/Spin"
    backup:
      plugin: "nav2_behaviors/BackUp"
    wait:
      plugin: "nav2_behaviors/Wait"
    global_frame: odom
    robot_base_frame: base_link
    transform_tolerance: 0.1
    simulate_ahead_time: 2.0
    max_rotational_vel: 1.0
    min_rotational_vel: 0.4
    rotational_acc_lim: 1.5

# ============================================================
# BT NAVIGATOR (orchestrates planner + controller + behaviors)
# ============================================================
bt_navigator:
  ros__parameters:
    use_sim_time: true
    global_frame: map
    robot_base_frame: base_link
    odom_topic: /robot_a/odom/nav
    bt_loop_duration: 10
    default_server_timeout: 20
    # bt_navigator does NOT fallback to ament_index when these are empty —
    # it loads "" → "Empty Tree" → instant Goal failed. Must be absolute.
    # Use the local-extracted prefix; falls through to /opt/ros/humble if
    # the local prefix path doesn't exist (rare; install via apt is fine).
    default_nav_to_pose_bt_xml: "/home/ylhc/Desktop/Collab_QRC/.local_deps/nav2_humble/extract/opt/ros/humble/share/nav2_bt_navigator/behavior_trees/navigate_to_pose_w_replanning_and_recovery.xml"
    default_nav_through_poses_bt_xml: "/home/ylhc/Desktop/Collab_QRC/.local_deps/nav2_humble/extract/opt/ros/humble/share/nav2_bt_navigator/behavior_trees/navigate_through_poses_w_replanning_and_recovery.xml"
    # Full default Nav2 BT plugin set — matches nav2_params.yaml. Trimming
    # individual plugins triggers "Node not recognized" errors when the
    # bundled XML behaviour trees reference any missing plugin.
    plugin_lib_names:
      - nav2_compute_path_to_pose_action_bt_node
      - nav2_compute_path_through_poses_action_bt_node
      - nav2_smooth_path_action_bt_node
      - nav2_follow_path_action_bt_node
      - nav2_spin_action_bt_node
      - nav2_wait_action_bt_node
      - nav2_assisted_teleop_action_bt_node
      - nav2_back_up_action_bt_node
      - nav2_drive_on_heading_bt_node
      - nav2_clear_costmap_service_bt_node
      - nav2_is_stuck_condition_bt_node
      - nav2_goal_reached_condition_bt_node
      - nav2_goal_updated_condition_bt_node
      - nav2_globally_updated_goal_condition_bt_node
      - nav2_is_path_valid_condition_bt_node
      - nav2_initial_pose_received_condition_bt_node
      - nav2_reinitialize_global_localization_service_bt_node
      - nav2_rate_controller_bt_node
      - nav2_distance_controller_bt_node
      - nav2_speed_controller_bt_node
      - nav2_truncate_path_action_bt_node
      - nav2_truncate_path_local_action_bt_node
      - nav2_goal_updater_node_bt_node
      - nav2_recovery_node_bt_node
      - nav2_pipeline_sequence_bt_node
      - nav2_round_robin_node_bt_node
      - nav2_transform_available_condition_bt_node
      - nav2_time_expired_condition_bt_node
      - nav2_distance_traveled_condition_bt_node
      - nav2_single_trigger_bt_node
      - nav2_goal_updated_controller_bt_node
      - nav2_is_battery_low_condition_bt_node
      - nav2_navigate_through_poses_action_bt_node
      - nav2_navigate_to_pose_action_bt_node
      - nav2_remove_passed_goals_action_bt_node
      - nav2_planner_selector_bt_node
      - nav2_controller_selector_bt_node
      - nav2_goal_checker_selector_bt_node
      - nav2_controller_cancel_bt_node
      - nav2_path_longer_on_approach_bt_node
      - nav2_wait_cancel_bt_node
      - nav2_spin_cancel_bt_node
      - nav2_back_up_cancel_bt_node
      - nav2_assisted_teleop_cancel_bt_node
      - nav2_drive_on_heading_cancel_bt_node
      - nav2_is_battery_charging_condition_bt_node

# ============================================================
# LIFECYCLE MANAGER (brings all nav2 servers up)
# ============================================================
lifecycle_manager_navigation:
  ros__parameters:
    use_sim_time: true
    autostart: true
    bond_timeout: 0.0
    node_names:
      - "controller_server"
      - "planner_server"
      - "behavior_server"
      - "bt_navigator"
